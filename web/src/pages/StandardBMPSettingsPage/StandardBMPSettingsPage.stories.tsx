import type { Meta, StoryObj } from '@storybook/react'

import StandardBmpSettingsPage from './StandardBmpSettingsPage'

const meta: Meta<typeof StandardBmpSettingsPage> = {
  component: StandardBmpSettingsPage,
}

export default meta

type Story = StoryObj<typeof StandardBmpSettingsPage>

export const Primary: Story = {}
